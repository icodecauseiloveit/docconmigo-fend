name: Print Secret for Verification

on:
  push:
    branches:
      - main

jobs:
  print_secret:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Print Secret Value
      run: |
        echo ${{ env.FTP_SERVER }}
