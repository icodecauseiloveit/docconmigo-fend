on: 
  push:
    branches: 
      - main 
    
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
      
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
  
    - name: Use Node.js 18
      uses: actions/setup-node@v3
      with:
        node-version: 18
          
    - name: Install Dependencies
      run: npm install
  
    - name: 🔨 Build Project
      run: npm run build
      
    - name: 📂 Upload to FTP Server.
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.docconmigo.com
        username: nodejs@docconmigo.com
        password: 1i;cI9AWQI;+
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/src/**
          .git*
          README*
          pack*
